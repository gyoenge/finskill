"use client";
import { AssetIcon, PioSays } from "@/components/Brand";
import { TimelineLoading } from "@/components/timeline/TimelineStore";
import { useOpportunities } from "@/lib/domain/useOpportunities";
import type { RankedOpportunity } from "@/lib/domain/opportunity-rank";
const CATEGORY: Record<string, { label: string; asset: string ;}> = { housing: { label: "주거", asset: "event-housing" }, employment: { label: "일자리", asset: "event-career" }, education: { label: "교육·장학", asset: "event-education" }, asset: { label: "자산형성", asset: "event-savings" }, finance: { label: "금융·복지", asset: "utility-opportunity" } };
export default function OpportunitiesPage() { const { ranked, bundle, loading, error } = useOpportunities(); return <div className="space-y-6"><header className="page-header opportunity-header"><div><p className="eyebrow">OPPORTUNITIES FOR YOU</p><h1>지금의 기회</h1><p>나의 계획에 가까운 지원정보부터 살펴보세요.</p></div><AssetIcon name="utility-opportunity" size={100} /></header>{bundle && !loading && <div className="opportunity-summary"><span>금융정보 <strong>{bundle.total}개</strong> 확인</span><span aria-hidden="true">→</span><span>나이 · 지역 · 계획 · 마감 기준</span><strong>관련 정보 {ranked.length}개</strong></div>}{loading && <TimelineLoading />}{error && <PioSays>정보를 불러오지 못했어요. 잠시 후 다시 방문해 주세요.</PioSays>}{!loading && !error && ranked.length === 0 && <PioSays>지금 보여드릴 기회를 찾지 못했어요. 새로운 계획이 있다면 타임라인에 추가해보세요.</PioSays>}<ul className="grid gap-5 xl:grid-cols-2">{ranked.slice(0, 24).map(item => <OpportunityCard key={item.opp.id} r={item} />)}</ul>{bundle && <p className="opportunity-source">출처: {bundle.sources.map(s => `${s.label} ${s.count}건`).join(" · ")}<br />관련성에 따른 안내이며, 신청 가능 여부는 공식 공고에서 확인해주세요.</p>}</div> ;}
function OpportunityCard({ r }: { r: RankedOpportunity ;}) { const { opp, reasons, dday } = r; const meta = CATEGORY[opp.category] ?? CATEGORY.finance; const amount = (opp.eligibility as { amount?: string ;}).amount; return <li className="card-soft opportunity-card"><div className="flex items-start gap-4"><span className="asset-tile"><AssetIcon name={meta.asset} size={52} /></span><div className="min-w-0 flex-1"><div className="mb-2 flex flex-wrap gap-2"><span className="status-badge confirmed">{meta.label}</span>{dday !== null && <span className="status-badge deadline">D-{dday}</span>}</div><h2>{opp.title}</h2><p className="provider">{opp.provider}</p></div></div>{amount && amount.length < 40 && <span className="status-badge goal">{amount}</span>}{reasons.length > 0 && <div className="reasons"><p>내 계획과 어떤 관련이 있나요?</p><ul>{reasons.map((why, i) => <li key={i}><span className="text-fin-green-700">✓ </span>{why}</li>)}</ul></div>}{opp.officialUrl ? <a href={opp.officialUrl} target="_blank" rel="noopener noreferrer" className="button button-secondary">공식 정보 확인하기 ↗</a> : <p className="text-sm text-ink-500">신청 경로는 제공 기관에 확인해주세요.</p>}</li> ;}
